                <!-- Interactive ECharts History Charts -->
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
                    <div class="glass-panel p-6 rounded-xl">
                        <div class="flex items-center justify-between mb-4">
                            <h3 class="text-xs font-bold text-slate-400 uppercase tracking-wider">LHB Institution & Sentiment History</h3>
                            <span class="text-xs text-slate-500 font-mono">Days: ${data.length}</span>
                        </div>
                        <div id="sentiment-history-chart" class="w-full h-80"></div>
                    </div>
                    <div class="glass-panel p-6 rounded-xl">
                        <div class="flex items-center justify-between mb-4">
                            <h3 class="text-xs font-bold text-slate-400 uppercase tracking-wider">New High/Low Breadth</h3>
                        </div>
                        <div id="breadth-history-chart" class="w-full h-80"></div>
                    </div>
                </div>
            `;

            // Initialize Charts
            setTimeout(() => {
                const dates = data.map(d => d.date);
                
                // Chart 1: Sentiment & LHB
                const sentChart = echarts.init(document.getElementById('sentiment-history-chart'));
                sentChart.setOption({
                    backgroundColor: 'transparent',
                    tooltip: { trigger: 'axis' },
                    legend: { data: ['LHB Net Buy', 'ZZ Sentiment'], textStyle: { color: '#94a3b8' } },
                    xAxis: { type: 'category', data: dates, axisLine: { lineStyle: { color: '#334155' } } },
                    yAxis: [
                        { type: 'value', name: 'Net Buy (亿)', axisLine: { lineStyle: { color: '#334155' } }, splitLine: { show: false } },
                        { type: 'value', name: 'ZZ Score', axisLine: { lineStyle: { color: '#334155' } }, splitLine: { lineStyle: { color: 'rgba(51,65,85,0.1)' } } }
                    ],
                    series: [
                        { name: 'LHB Net Buy', type: 'bar', data: data.map(d => (d.dt_net_buy_wan/10000).toFixed(2)), color: '#10b981', opacity: 0.7 },
                        { name: 'ZZ Sentiment', type: 'line', yAxisIndex: 1, data: data.map(d => d.zz_sentiment_close), smooth: true, color: '#38bdf8' }
                    ]
                });

                // Chart 2: Breadth
                const breadthChart = echarts.init(document.getElementById('breadth-history-chart'));
                breadthChart.setOption({
                    backgroundColor: 'transparent',
                    tooltip: { trigger: 'axis' },
                    legend: { data: ['New High 20d', 'New Low 20d'], textStyle: { color: '#94a3b8' } },
                    xAxis: { type: 'category', data: dates, axisLine: { lineStyle: { color: '#334155' } } },
                    yAxis: { type: 'value', axisLine: { lineStyle: { color: '#334155' } }, splitLine: { lineStyle: { color: 'rgba(51,65,85,0.1)' } } },
                    series: [
                        { name: 'New High 20d', type: 'line', data: data.map(d => d.high20), smooth: true, color: '#f43f5e', areaStyle: { opacity: 0.1 } },
                        { name: 'New Low 20d', type: 'line', data: data.map(d => d.low20), smooth: true, color: '#38bdf8', areaStyle: { opacity: 0.1 } }
                    ]
                });
            }, 100);
        }
